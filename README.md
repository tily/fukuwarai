# fukuwarai

## Usage

```
# download twitter emoticon files
docker-compose run --rm app thor download

# organize emoticon parts
docker-compose run --rm app thor organize

# run web service
docker-compose up -d
```
